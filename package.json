{
  "name": "fizioterapi",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @fizioterapi/db prisma:generate",
    "db:migrate": "pnpm --filter @fizioterapi/db prisma:migrate",
    "db:seed": "pnpm --filter @fizioterapi/db prisma:seed"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
